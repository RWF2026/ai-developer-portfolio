# Where visit alerts are delivered.
# With a free Resend testing key (no custom domain), this MUST be the email
# on your Resend account. After you verify a domain, you can use any inbox.
NOTIFY_EMAIL=jashwanth.20231cse3024@presidencyuniversity.in

# Resend API key (never commit real keys — use Vercel Project Settings → Environment Variables)
RESEND_API_KEY=

# Free testing sender from Resend (change after verifying your own domain)
RESEND_FROM_EMAIL=Portfolio <onboarding@resend.dev>
